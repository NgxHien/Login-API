export interface ResponseCodeItem {
    code: number;
    alias: string | undefined;
    data: unknown;
    msg: string;
}
export default class CodeMap {
    private mapper;
    constructor();
    install(codeList: ResponseCodeItem[]): void;
    get(key: number | string): ResponseCodeItem | undefined;
    has(key: number | string): boolean;
}
