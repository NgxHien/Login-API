import { ResponseCodeItem } from './codemap';
import { I18nList } from './i18n';
declare type Response = Pick<ResponseCodeItem, 'code' | 'data' | 'msg'>;
export default class ResponseFactory {
    private codeMap;
    private i18nMap;
    constructor();
    setMap(codeList: ResponseCodeItem[]): void;
    i18n(i18nList: I18nList): void;
    get(key: string | number, langCode: string | undefined): Response | undefined;
}
export {};
